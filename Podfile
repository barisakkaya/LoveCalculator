# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LoveCalculator' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoveCalculator
  pod 'lottie-ios'
  pod 'Material', '~> 3.1.0'
  pod 'Alamofire'
  pod 'SnapKit', '~> 5.0.0'
  pod 'Hero'
end
